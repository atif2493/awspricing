# awspricing - optional env for local dev (docker compose)
# Copy to .env and fill in if not using IAM role / instance profile.

# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=us-east-1

# Optional: cache TTL in seconds (default 86400 = 24h)
# PRICING_CACHE_TTL_SECONDS=86400
