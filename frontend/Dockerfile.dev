# Frontend dev: run Vite dev server with source mounted. Port: 5173 (mapped 3001 in compose).
# v1.0 — Mount ./frontend so local changes take effect immediately.
FROM node:20-alpine
WORKDIR /app
COPY package.json package-lock.json* ./
RUN npm install
COPY . .
EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
